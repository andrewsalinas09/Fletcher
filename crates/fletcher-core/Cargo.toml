[package]
name = "fletcher-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Fletcher's engine: Equalizer APO config parsing, filter math, device layer"

[dependencies]
wasapi = "0.24.0"
windows = { version = "0.62", features = ["Win32_System_Com"] }
windows-core = "0.62"
winreg = "0.55"
