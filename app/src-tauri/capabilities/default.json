{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "IPC capability for every Fletcher window. The glob is deliberate: satellite windows (history, diff, and every future pop-out room) are created at runtime with new labels, and a label missing here gets NO event/invoke access — it renders but sits dead. Naming windows individually made that a recurring class of bug.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-set-cursor-position",
    "core:window:allow-inner-position",
    "core:window:allow-create",
    "core:window:allow-set-focus",
    "core:window:allow-show",
    "core:webview:allow-create-webview-window",
    "dialog:default"
  ]
}
