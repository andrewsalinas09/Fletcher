# Worklog

Append-only session journal. Newest entry first.

**Entry template**
```
## YYYY-MM-DD — short title
- **Did:** what changed in the repo/design
- **Learned:** surprises, verified facts, dead ends
- **Next:** the single most useful next step
```

---

## 2026-08-23 (night) — Phase 3 opens: the track engine's foundation (Clip Studio M1)

- **Did:** Clip Studio planned end-to-end (plan file: milestones M1–M8; decisions: **ffmpeg is the native decode path** — fetch-on-demand tools per ADR-0008, GPL-fine as separate processes; **yt-dlp URL import is committed scope** (user); clip library = **SQLite**; generator = library items + calibration; Q-21 stays open, no EQ-tab chip). **M1 shipped:** `dsp::BiquadState`/`ChainProcessor` (DF2T realtime path, cross-validated against `magnitude_db` by sine-RMS — no golden data), `signal.rs` (white/pink/sine/sweeps/band-noise, hard-capped −12 dBFS, deterministic seeds; `gen` is a reserved word in edition 2024), `playback.rs` (the spike productionized: format ladder, aligned-retry with client re-acquisition, both modes, typed errors, **mandatory 300 ms ramp-in + fade-outs everywhere** — timed signals fade their last 300 ms, stop requests fade 150 ms in-engine; nothing can ever cut). Settings gained the engine test tone (SYSTEM) and the **Q-16 pink-noise leveling flow** (REFERENCE, shared path so the volume knob stays live; interlocked with ABX both ways).
- **Learned:** `referenceDb` is a **headroom budget, not a taste choice** — matching only works if a chain's peak-above-mean fits under 0 dBFS, so −8 stays default and the physical knob absorbs it (recorded in the Settings note). TB-08 v1 fix filed: detect the clip-capped match and surface the shortfall (scheduled M3).
- **Did (later):** **M2 + M3 shipped, then re-aimed by two user rulings (ADR-0011).** Tools manager (ffmpeg/yt-dlp fetched on demand with consent + progress; Settings TOOL PATHS shows every resolved path incl. APO), ffmpeg decode → per-(track,rate) PCM cache (memcpy load; "decoding" only when ffmpeg actually runs), SQLite library v1 (tracks + flat LUFS measured once), Clip Studio tab v0 (library rail, import, delete), transport in the A/B bar (play/pause/stop/seek/time). M3 built the dual-bus in-engine A/B (both buses always processed, 15 ms equal-power crossfade, true-LUFS trim, ab_flip third branch) — then the ruling: **curation BYPASSES by default** (exclusive, no EQ, the track itself level-matched toward the reference via stored LUFS), **through-your-EQ is the opt-in regular player** (shared path, zero special cases), and the dual-bus machinery is **testing mode, dormant until the Lab (phase ③)**. Session handoffs serialized (stop waits for device release; serials keep stale "ended" events from clobbering the UI — fixed a live multi-stream overlap bug the user caught). TB-08 v1 shipped: the A/B bar says "matched — short X dB (clip cap)" instead of capping silently.
- **Learned:** The first curation design (shared-path-always) broke on its own edge case (engine A/B vs the EQ panel's truth) — the user's two-mode cut (bypass vs through-EQ) dissolves every special case instead of patching them. Stream handoff needs an explicit wait: raising a stop flag is not a released device.
- **Did (M4 + M5, same night):** **The timeline became a mini NLE** (user's bar: "like DaVinci Resolve"): adaptive timecode ruler, zoom-around-cursor to single samples (peak buckets → raw-sample windows → interpolated curve, ONE bridged renderer — no gaps, no mode pop), drag-pan, click-seek, playhead follow that never fights a pan. **I/O region + clips**: I/O keys, draggable edges (resize cursor), loop solo (engine wraps sample-accurately), Save clip → SQLite (clips + clip_tags), rail rows with tag-colored dots, **inspector strip** (contextual, artboard anatomy: name ✎ / range / band+free tags / note / greyed Add-to-battery — batteries belong to the Lab). **The scopes (M5)**: Rust STFT spectrogram lane (log-freq, boost-orange-on-ink, params = room settings: window 1k/2k/4k + floor hot/normal/deep, memoized per combo), FFT-at-playhead pane with the EQ curve overlaid + boost/cut shading, Spec/Wave/FFT combinable toggles, **pop-out scope windows** (self-feeding satellites: broadcast events + direct invokes — no main routing to break; popping out removes the pane inline until the window closes; zoom linked across windows). **Keys everywhere**: space/C/I/O in satellites via focus-scoped OS shortcuts with press+release forwarding (Q-20). **Velocity scrub** (two iterations to Resolve-truth): held C = the cursor IS the playhead; the audio thread chases it at motion velocity — variable rate, reverse included, amplitude ∝ speed, silence at stillness. Rooms own their settings (⚙ pattern: EQ ordering; Clip Studio timecode decimals/scopes/spectrogram params/play method). Dev profile: dependencies compile at opt-level 2 (FFT in debug was unusable).
- **Learned:** Naive per-column peak rendering produces dashed waveforms (bridge neighbors — the Audacity/Resolve trick). Scrub is a position-chasing variable-rate player, not seek+play — the sound must come FROM the motion. Tauri global shortcuts report press AND release, which makes held-key semantics possible in satellites.
- **Next (handoff — design already settled, don't re-derive):** **The signal generator workflow** (user-sequenced before spectrogram moments). Design: a "+ Generate" button beside "+ Import track" opens a center modal (hist-panel style, smaller) — pick primitive (white/pink/sine/sweep log/linear/band-limited noise), parameters, duration (seconds), level in dB (seg −30/−24/−18/−12; `signal::MAX_AMP` caps at −12 regardless), **live Preview** (loops through the shared path via the CAL_NOISE-style aux stream — REUSE that static so only one aux stream ever runs; ABX/track interlocks already stop it), and **Add to library** → a track row `kind='signal'` with `signal_params` JSON (column already exists) and an auto title ("Pink noise · 30 s"). The key architectural move: factor `prepare_pcm(id, rate, on_decode)` out of track_play/pcm_for/track_waveform — it branches on kind: file → ffmpeg ensure_pcm; **signal → synthesize `signal::Signal` (seed = track id, deterministic = provenance) into the SAME cache file naming `{id}-{rate}.f32`** — then playback, waveform, spectrogram, clips, tags all work on signals with zero further code. Mark signal rows with a ∿ glyph in the rail. After that: spectrogram box-select moments (Q-18 capture half), then yt-dlp (M7), batteries in the LAB not Clip Studio (user ruling).
- **Untested by the user at handoff:** spectrogram window/floor settings, scope-popout keys (space/C via OS shortcuts), cross-window zoom sync, pop-out-removes-pane round-trip, history-popout title fix. **Velocity scrub: VERIFIED and loved** ("I've never heard anything so smooth") — the chase-resampler design is settled. The user then asked for the feel knobs to be theirs: tau + max speed are now typed-in Clip Studio room settings ("Scrub feel", localStorage `fletcher.scrubtau`/`scrubmax`, defaults 60 ms · 8×) pushed live into engine statics via `track_scrub_params` — they apply mid-scrub. Amp = |speed| and teleport 2 s remain fixed design, not knobs.
- **Did (next session, same day):** Cursor readouts drawn on every plot canvas (timeline: time+amplitude/dBFS; spectrogram lane: time+freq+tile dB, "≤floor" when saturated; FFT: freq+cursor dB+signal dB) — satellites inherit them for free. Double-click a clip = open AND zoom to it (waits for the waveform; synced windows follow). I/O region now reaches the spectrogram pop-out (main broadcasts `io-region`, answers `scope-hello`) and Esc clears it from there too (added to the focus-scoped OS shortcuts). **The signal generator shipped per the settled design:** `prepare_pcm` gate factored out (files→ffmpeg, signals→deterministic synthesis, seed = track id, 100 ms edge fades, same `{id}-{rate}.f32` cache), `signal_create`/`signal_preview` (live-retuning preview on the CAL_NOISE aux stream, `sig-preview-ended` un-sticks the button), ∿ Generate modal (type/params/duration/level −30..−12, typed inputs), rail split into IMPORTED/CREATED with ∿ glyphs + recipe metadata lines. Filed in FEATURES: future Settings PALETTE & FONTS section (every color/font user-overridable via the ADR-0010 tokens; canvases must read tokens first).
- **Did (Q-25 ships, same session):** Advanced signal composition per user rulings: **mix** (SigSpec grew recursive `layers`, one level deep; `sig_build` flattens with decorrelated per-layer seeds; the −12 dBFS cap binds the SUM in both render and preview — `SignalSource` now takes a `Vec<Signal>` stack), **modulators** (`Signal::with_am`/`with_fm` in core — tremolo on anything, vibrato adds to the instantaneous frequency of tonal kinds; new core test), and the **`{ }` text tier** (recipe JSON editor with `signal_validate` on apply — ruled the future API/MCP surface, so SigSpec is a public schema now: additive only). Mix UI is master–detail: layer list + per-layer type/params/level. `seq` ruled OUT of the generator (batteries own sequencing) and the inspector's greyed "Add to battery" removed. Earlier same session: sweep pacing UI (One pass default / Repeat opt-in), wider freq inputs, preview stuck-button race fixed with serials (a replaced stream's late "ended" was clobbering its successor).
- **Doc debt owed (the M8 sweep, do soon):** GLOSSARY entries for clip / moment / loop / signal generator / track session / library (vocabulary-audit law — these are user-facing words with no docs behind them); ARCHITECTURE implementation-state refresh (clip/scope/scrub command surface, track-pos/track-state/scope-* events, the scope-window satellites, prepare-pcm/caches); FEATURES yt-dlp line. Deferred taste items the user named: waveform look "not perfect but fine for now" (consider Resolve filled-body), LHS rail "expand a little later" (artboard search field, tag chips, moments rows), spectrogram deep-zoom re-render (overview tiles only today).

## 2026-08-23 (evening) — Q-24 ships: the history inspector

- **Did:** Built the inspector end-to-end in one pass (fresh session, planned from the handoff docs). **Rust:** `chain_curves` (batched responses + matched preamps for arbitrary chains — no command could answer for a non-live chain before), `preview_chain` (level-matched fletcher.txt-only audition: no preset write, no abSide reset, refused mid-ABX), `preset_create_from_chain` (auto-suffixed promote), and the ABX engine generalized — `abx_start` optionally takes two chains + names (classic preset-vs-flat unchanged), auditions go through `activate_chain` so level matching between any two chains is free, a response-difference guard replaces the filter-count check, and results now carry both chains + `referenceDb` as provenance (labeled replay of node sessions works from the record). Extracted `chain_of` as the shared validate/clamp adapter. **UI:** right-side inspector inside the shared `HistoryTree` (both windows — snapshots now ride `hist-sync`; pop-out mutations/audio route via `hist-cmd`, curve reads invoke directly): rename/note/pin (persisted, tolerant v1 format), "vs: parent ▾" chip with pick-mode, parametric diff (greedy kind+nearest-log-Fc pairing; leftover pairs at ~same Fc read as type changes), the level-matched difference curve with boost/cut shading + faint context curves + its own auto/±3/±6/±12 scale chips, multi-node compare (Ctrl+click; overlaid color-coded diff curves vs the shared base, legend rows with mean |Δ|), listen previews with restore-on-unmount/-on-move/-on-popout-destroy (TB-25), node-vs-node blind test surfacing the main window, promote + copy-as-APO (formatter unified with filter Ctrl+C). Edge thickness on the canvas = mean |audible diff| per edge. Click semantics per user: plain click keeps jump+inspect, Alt+click inspects silently, Ctrl+click compares (matches strip convention). TB-25/TB-26 filed; Q-17 closed; Q-24 marked shipped.
- **Did (live passes, same session):** Two taste passes with the user on the running app: all diff headers/mean-|Δ|/preamp readouts cut (level matching makes numbers dead weight), base picker became a direct pick-mode chip, previews became a HEAR #12|#11 segmented toggle (A/B idiom; button trios read as mess), "promote"→"save as preset", jump button dropped (clicking the node IS jump), diff scale ±3/±6/±12/±18/±30, Esc deselects filters on the EQ tab, **concrete node ids everywhere** (blind test #12 vs #11; ids drawn inside the circles, long labels truncated with full name on hover), undo/redo move the inspector selection (they're literally clicks on the graph). **The difference graph became the second pop-out** (⇱; main resolves the last-touched compare against the live tree — default current-vs-parent continuously shows "what did my last gesture change" while sculpting). **Drag-graft** (user idea): drag any node onto any other → its exact sound lands as ONE clean step ("N edits in one"), non-destructive squash, becomes current; works from the pop-out via hist-cmd.
- **Learned:** The backend never modeled history — trees are opaque JSON to Rust, so the whole inspector rides on chains-as-arguments commands. `activate_chain` was already the perfect preview/ABX primitive, just unexposed. **Platform law #2 (bit twice):** Tauri capabilities are per-window-label; an unlisted label renders with silently dead IPC — fixed as a class with `"windows": ["*"]`. **Dev law (took two rounds to name right):** vite fast-refresh never re-runs `[]`-effects, so long-lived listeners capture STALE closures — the popout→main command bus ran old code (first missing the graft case entirely, then showing a removed notice) while main-window paths ran current code. Class fix: trampoline refs (`busRef`/`keyRef`) — the registered callback is permanent, the ref body is refreshed every render. Every future long-lived listener follows this pattern. New repo rule #10: every feature is traced through the satellite path before it's called done. Verified end of session: main + both pop-outs all working (drag-graft from pop-out confirmed by user after fresh start); cargo test (27), clippy, fmt, tsc clean.
- **Did (later, same session):** **Settings v1 shipped** from the approved artboard: filter ordering (freq/importance/file — display-only strip sort), the **level-matching honesty switch** (off = clip-safety only; A/B bar shows "unmatched — louder side wins", trial room warns, `levelMatched` recorded in every session's provenance; refused mid-ABX), reference loudness as an editable value (calibration flow arrives with the generator, Q-16), Start with Windows (tauri-plugin-autostart), A/B hotkey display, Standard/Advanced mode wired to the Fingerprints tab (grey-don't-hide per ADR-0002, with the artboard's demo box), APO detection status. Roadmap re-sequenced by the user: Settings → **Clip Studio incl. the shared signal generator** → Lab expansion after.
- **Next:** Clip Studio (large — plan carefully): track engine (ADR-0009 exclusive-mode path), shared signal generator, waveform/clip curation UI. Residual watch: pop-out rename/note typing vs the no-DOM-keys law.

## 2026-08-22 — Design session: five iterations to a settled EQ-tab direction

- **Did:** Built a design canvas (claude.ai/code/artifact/759d400e-…) with three directions from real HD650 data; user killed Swiss, chose Console, then drove five iterations: v3 curve/editor split, v4 transposed horizontal strip + tooltip-not-panel + bidirectional graph↔strip editing, v5 arc-fill gain gauges (needles rejected as unglanceable) and type-as-dropdown (name · curve glyph · caret). Persistent A/B bar replaced the floating Blind-test button. Settings artboard approved early and untouched since. Vocabulary audit: invented labels (PRINTS, BENCH, response-curve toggle) purged after user flagged them as unmappable to our docs. New ideas filed: pop-out windows with synced state (Q-20), post-"Load a track" flow (Q-21).
- **Learned:** The user's design taste is concrete and fast when given real candidates to react against; "show 3, iterate on reactions" works. Redundant views (rail duplicating graph) read as mess; amount-of-ink beats needle-angle for glanceability; every UI word must trace to FEATURES.md or it reads as hallucination.
- **Next:** ADR-0010 (visual direction); port v5 into the app; design Listening Lab + Clip Studio artboards (resolves Q-21).

## 2026-08-23 (session close) — handoff hygiene
- **Did:** Post-sweep addendum: verified all project files live in the repo (session had been launched from `C:\Users\andre`, not the repo — files were always written by absolute path; tree clean, pushed). Seeded the repo-launch memory dir (`.claude/projects/C--Users-andre-RustroverProjects-Fletcher/memory/`) with project snapshot, working-style, and user profile. **Sessions should be launched from the repo root** so this CLAUDE.md auto-loads. Q-24 nuance filed (inspector complements the EQ page; compared metadata fields).
- **Next:** unchanged — Q-24 inspector build first.

## 2026-08-23 (later) — AutoEQ, reference loudness, the undo graph epic, multi-window

- **Did:** **AutoEQ import** (fetch-on-demand per ADR-0008: cached INDEX.md + per-file cache, stale-if-error; preset files are native APO grammar; search UI in preset menu; onboarding no longer needs Peace). **Preset rename** (inline ✎). **Global reference loudness** (user's insight): everything — flat, every preset, mid-edit chains — normalizes via `matched_preamp` to one reference (default −8 dB, `referenceDb` in state.json; Settings noise-calibration will own it, Q-16); replaces per-chain B-matching; level-matched *sculpting* included. **Output-device switching** via IPolicyConfig on the device chip. **Filter copy/paste**: Ctrl+C writes real APO lines to the OS clipboard, Ctrl+V parses any clipboard text via `parse_filters` (multi-line = whole presets paste). **Multi-select** (Ctrl toggle / Shift range in strip order): group drag preserves dB deltas + log-Fc ratios; group wheel-Q/copy/delete; per-selected contribution curves. **The undo graph epic**: gesture-completion nodes only (drop / Enter / wheel settled 1s) → branching tree → family-tree canvas sub-panel (84vw, text-in-circles, cursor-anchored zoom via layout-effect correction, drag-pan, centered open, armed subtree-delete confirms w/ click-away) → **first pop-out window** (shared HistoryTree component; main owns truth; satellites command via emitTo) → **persistence** (`history/<preset>.json`, auto-save, resume-divergence nodes) → **export/import** (dialog plugin) → **the rail** (jump fixes the undo/redo track root→leaf via latest-created children). **Platform law:** runtime-created WebView2 windows receive NO DOM keyboard events — popout undo/redo runs as focus-scoped OS shortcuts registered in Rust (register on focus, unregister on blur/close). Error routing split (notice bar vs fatal APO alert); selection-highlight killed app-wide; drag-scale wars ended by the interaction-inert scale control (auto/±6/±12/±18/±30) — principle recorded: automation and direct manipulation never share an axis. Q-24 designed (history inspector: simple right panel, difference-curve core, any-N-node compare, NO audibility claims — "the user proves it").
- **Learned:** The user considers the undo graph "insanely useful" — it reframed history from undo-mechanism to experiment log. First-ever verified result stands: 13/16, p = 0.011. CLAUDE.md refreshed from "design phase" to current reality.
- **Next:** Q-24 inspector build (includes node-vs-node ABX, Q-17's last residual); Settings tab; adaptive ABX; preference voting. Dev note: `cd app && npm run tauri dev`; Peace still installed alongside — its EQ should be OFF when auditioning Fletcher chains.

## 2026-08-23 — The Listening Lab ships; first verified result

- **Did:** Built the ABX engine end-to-end: exact binomial stats + xorshift in core (verified against hand-computed values); server-side-only session state (real blinding — the UI never holds X's assignments); audition/vote commands through the level-matched write path; provenance-journaled sessions with recorded mid-session score reveals (TB-24); persisted labeled logs; Lab home (prominent trial controls, the record with plain-language verdicts, labeled replay, run-again-same-N); full-window trial room (keyboard-first, hotkey cycles A/B/X, neutral dots, bias-cautioned reveal). Also: output-device switching via IPolicyConfig on the device chip; premature A/B-bar buttons removed.
- **Learned:** **First real session, run by the user on their HD650 correction: 13/16, p = 0.011 — "You heard it."** Their debrief: genuinely hard ("took me 1.5 entire songs"); treble passages indistinguishable; bass "even just slightly" different. Every observation maps to planned machinery: content dependence → clip battery (short targeted loops instead of waiting for revealing passages); per-band audibility → per-tag statistics; effort → the whole reason honest testing needs tooling.
- **Next:** Adaptive/sequential mode (proper corrections), preference voting, AutoEQ import, Settings tab. Phase 3 brings clips to the Lab.

## 2026-08-23 — Phase 1 closes; the thesis validates itself

- **Did:** Tray + global Ctrl+Shift+A + level-matched A/B (Q-06 estimation v1: log-grid mean ≈ pink-noise weighting, applied as B's preamp). Close-to-tray, deterministic side restore at launch. Earlier same session: y-scale saga resolved by first principles — explicit interaction-inert scale control (auto/±6/±12/±18/±30); automation and direct manipulation must never share an axis. Custom type picker with glyphs + tooltips; deliberate-hover teaching layer; undo/redo; drag jitter, rename contention, and coordinate-mapping bugs all fixed through live user testing.
- **Learned:** THE moment: the user self-blinded (flipped the hotkey repeatedly while looking away) and found their HD650 correction — obvious when sighted — became "barely" distinguishable once level-matched and blind. Their words: "wayyy harder than I thought… you really have to listen closely." The product's core claim (loudness bias + sighted bias dominate perceived EQ differences) reproduced on its builder, night one. Also noted content-dependence ("this track didn't have a lot of bass") — the exact motivation for the clip battery.
- **Next:** Phase 2: blind ABX with real statistics (the trial room) — turn "barely" into a p-value. Then Settings tab, VISION rewrite.

## 2026-08-22 — The app becomes an EQ: DSP port, live editing, presets

- **Did:** Ported the Console design into the running app (ADR-0010 CSS tokens, real components). fletcher-core grew dsp.rs (RBJ biquads, response, interaction-aware auto-preamp — TB-06 tested), devices.rs, presets.rs (23 core tests total). Commands: eq_state (live config → computed curves), set_fletcher_chain (chain → auto-preamp → atomic fletcher.txt write → APO hot-reload), preset suite (switch/create-from-live/duplicate/delete; active preset persisted, edits saved into it). UI: drag handles (throttled 60 ms live writes — audible while dragging), wheel-Q, per-filter bypass toggle, type dropdown, add/delete, preset menu with duplicate-from-live (turns Peace-owned locked filters into an editable chain), double-EQ warning chip, push-based auto-refresh via notify watcher (Refresh button removed), dynamic y-axis. Q-23 filed (rich preset management deferred); product principle recorded: anything non-trivial becomes its own complete room.
- **Learned:** Optimistic UI + throttled backend round-trips + watcher-refresh suppression during drags gives smooth live sculpting with zero UI-side DSP. serde enum field casing needs rename_all_fields (caught by user testing the live app).
- **Next:** Tray + global hotkey A/B (Phase 1 exit criterion), then per-device profiles and the Settings tab (ordering toggle).

## 2026-08-22 — Phase 1 opens: workspace, Tauri scaffold, config engine v1

- **Did:** Restructured to a cargo workspace: `crates/fletcher-core` (engine lib; spike moved to examples) + `app/` (Tauri 2 + React/TS via create-tauri-app, product name Fletcher, identifier net.asal.fletcher). Built the config engine's first component: a lossless line-preserving APO config parser (`config.rs`) — raw bytes kept per line, parsing as a view, byte-for-byte round-trip guaranteed; parses Preamp/Filter (12 kinds)/Include/Device/Channel, everything else degrades to Unknown. 7 integration tests green against real fixtures copied from the live APO install (Peace HD650 preset, root config, REW exports, multichannel). CI updated for workspace + frontend build. Mid-session feature idea filed: moment isolation + spectral similarity + future MCP agent hook (Q-18).
- **Learned:** REW exports (which APO reads and users Include) use European locale numbers — decimal commas and dot thousands-separators (`8.000` = 8000 Hz). Comma lines safely degrade to Unknown; the dot-thousands ambiguity needs REW-dialect detection before Fletcher ever interprets such files → TB-22.
- **Next:** Tauri command surface: read APO config via fletcher-core and render it in the UI (first end-to-end slice), then device layer + fletcher.txt writer + hotkey A/B.

## 2026-08-22 — Development kickoff: ADR-0007/0008, license, both spikes done

- **Did:** Kickoff decisions via user Q&A: include-line wiring (ADR-0007), AutoEQ fetch-on-demand (ADR-0008), spikes-first, CI-from-start; Apache 2.0 license added. Live spike results on the dev machine (APO 1.4.2 + Peace, HD650, Schiit Modi 3+): config dir writable without elevation (Users: FullControl); Fletcher wired in as `Include: fletcher.txt` alongside Peace's line; hot-reload through included files confirmed audibly — "basically 0 latency" at 1 s toggle cadence; WASAPI exclusive mode confirmed to bypass APO (Q-04) via first Rust code (`src/bin/spike_bypass.rs`, wasapi 0.24) — but it evicts other apps ungracefully (Apple Music "error playback", no auto-recover) and bypasses Windows volume (own gain stage mandatory, TB-20). GitHub Actions CI added (fmt/clippy/test, windows-latest).
- **Learned:** Peace zeroes peace.txt when toggled off (TB-09 churn is real). Exclusive-mode format negotiation on Modi 3+: 24-bit int @ 48 kHz.
- **Next:** Confirm ADR-0009 (exclusive-mode bypass as primary), then scaffold Phase 1 (Tauri app + config engine with round-trip tests).

## 2026-08-22 — Gap pass: profiles, loudness mode, history tree, safety

- **Did:** Seven-gap review with the user, all filed: per-device profiles with device-level auto-switch (headphone-level is a manual quick-switcher — analog jacks are invisible to Windows); loudness-compensation mode, off by default, anchored-reference design (Q-16, TB-21); preset history as a branching jumpable tree with blind A/B between any two nodes (Q-17); per-channel EQ / L-R balance from day one; mic calibration import (UMIK-2 / iMM-6 / OmniMic formats — user's real cal files as fixtures); sweep ear-safety (TB-20); code signing deferred to Phase 5 by user. Scope note: headphones first, room correction deferred.
- **Learned:** User owns a USB UMIK-2 — real hardware for Phase 4 measurement-engine development. Volume anchoring (RME-style reference point) makes loudness comp workable without a mic; USB-HID volume sync makes many DAC knobs visible.
- **Next:** Phase 0 exit: Q-04 spike, normative VISION rewrite, fingerprint-format sketch; then Phase 1 scaffold.

## 2026-08-22 — Idea-mapping session: the vision lands, six ADRs

- **Did:** Full feature brain-dump captured into FEATURES.md. Decisions locked: ADR-0002 (grey-don't-hide UI), ADR-0003 (flat reference + level matching by default), ADR-0004 (user-curated clip libraries / Clip Studio), ADR-0005 (Tauri 2 + TS/React shell), ADR-0006 (scope by sequencing, not cutting; everything-recorded provenance). Fingerprint Lab vision clarified: fingerprints are coupled HpTF∘ear measurements; personal libraries; export/import; bridging via shared headphones for at-home virtual auditioning (valid mainly <5–6 kHz). Q-01/02/06/07/11/13 resolved; Q-15 (fingerprint interchange format) opened; TB-13…TB-19 added. ROADMAP rewritten as six everything-ships phases. APO auto-install: no, for now.
- **Learned:** The reviewer-ecosystem/bridging idea makes the fingerprint *format* strategic — design it early (Phase 0/1), ship the Lab late. Preference vs. discrimination tests need separate statistical treatment (TB-15). Clip libraries being per-track + genre-tagged gives the stats engine content-type dimensions for free.
- **Next:** Q-04 research spike (how track-mode playback bypasses APO) + verify APO hot-reload semantics locally; then rewrite VISION as normative and scaffold Phase 1.

## 2026-08-22 — Founding session: name, stack, doc system

- **Did:** Project named **Fletcher** (after Fletcher–Munson). Rust core committed (ADR-0001). Documentation system bootstrapped mirroring the Humanity-Tech-Tree standard: CLAUDE.md rules, ADRs, OPEN-QUESTIONS (Q-01…Q-10), TESTBED (TB-01…TB-12), GLOSSARY, ROADMAP, research survey of the frontend landscape. Git remote added (github.com/andrewsalinas09/Fletcher).
- **Learned:** The modern-frontend niche is open — AQUA (Electron/React) was the only serious attempt and is abandoned. APO's config hot-reload makes system-wide A/B nearly free, but track-mode A/B **cannot** ride it (double-processing trap, TB-03) and needs an in-app engine. Level matching (LUFS) is what makes ABX statistics honest — and it's the feature no incumbent has.
- **Next:** Idea-mapping conversation with the user — capture the full feature vision, then rewrite VISION.md and start resolving Q-01/Q-02/Q-07.
